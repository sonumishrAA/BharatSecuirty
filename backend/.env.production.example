# Production Environment Variables
# Copy this to .env.production and fill in actual values

# Database (Render provides DATABASE_URL automatically)
DATABASE_URL=postgresql://user:password@host:5432/bharatsecurity?sslmode=require

# Server
PORT=3000
NODE_ENV=production

# JWT Secret (This is the one we planned - use this in Render Dashboard)
JWT_SECRET=bh4r4t-s3cur1ty-jwt-s3cr3t-k3y-2024-pr0duct10n

# CORS Origins (comma-separated list of allowed frontend URLs)
CORS_ORIGINS=https://bs-org-blog.netlify.app,https://bs-net-admin.netlify.app

# Rate Limiting (optional)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=100
